import type { Theme } from "@emotion/react";

export const theme: Theme = {
  color: {
    eventBlock: "#FEBE48",
    background: "#212121",
    font: {
      onBackground: "#ffffff",
      onForeground: "#000000",
    },
  },
};
